## iofogctl reconcile agent

Reconcile fog router/NATS platform for an agent

```
iofogctl reconcile agent NAME [flags]
```

### Examples

```
iofogctl reconcile agent my-agent
```

### Options

```
  -h, --help   help for agent
```

### Options inherited from parent commands

```
      --debug              Toggle for displaying verbose output of API clients (HTTP and SSH)
  -n, --namespace string   Namespace to execute respective command within (default "default")
  -v, --verbose            Toggle for displaying verbose output of iofogctl
```

### SEE ALSO

* [iofogctl reconcile](iofogctl_reconcile.md)	 - Retry async platform provisioning for an agent or service


